import React from 'react';
import { Text, View } from 'react-native';

export default function TaskList() {
    return (
        <View>
            <Text>This is the task list</Text>
        </View>
    );
}